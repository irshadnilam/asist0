# asisto

AI agent frontend. Built with TanStack Start, Bun, and Tailwind CSS.

## Development

```bash
cd app
bun install
bun run dev
```

Or from the project root:

```bash
make dev      # runs both backend + frontend
make dev-app  # runs frontend only
```
